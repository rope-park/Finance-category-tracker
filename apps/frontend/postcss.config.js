/**
 * PostCSS 설정 파일
 * 
 * CSS 후처리기 PostCSS의 플러그인 설정.
 * 브라우저 호환성을 위한 자동 prefix 추가 등 담당.
 * 
 * @author Ju Eul Park (rope-park)
 */

export default {
  plugins: {
    /** 브라우저별 CSS 접두사 자동 추가 플러그인 */
    autoprefixer: {},
  },
}
