/**
 * Tailwind CSS 설정 파일
 * 
 * 유틸리티 우선 CSS 프레임워크인 Tailwind CSS의 설정.
 * 커스텀 색상, 테마, 플러그인 등을 정의함.
 * 
 * 주요 설정:
 * - 컨텐츠 스캔 경로 설정
 * - 브랜드 색상 팔레트 정의
 * - 상태별 색상 (성공, 경고, 위험) 정의
 * - 반응형 디자인 지원
 * 
 * @author Ju Eul Park (rope-park)
 */

/** @type {import('tailwindcss').Config} */
export default {
  /** Tailwind가 클래스명을 찾을 파일들 */
  content: [
    "./index.html",                    // 메인 HTML 파일
    "./src/**/*.{js,ts,jsx,tsx}",     // 모든 소스 파일
  ],
  
  /** 테마 설정 */
  theme: {
    extend: {
      /** 커스텀 색상 팔레트 */
      colors: {
        /** 주 브랜드 색상 (파란색 계열) */
        primary: {
          50: '#eff6ff',   // 매우 연한 파란색
          500: '#3b82f6',  // 기본 파란색
          600: '#2563eb',  // 진한 파란색
          700: '#1d4ed8',  // 매우 진한 파란색
        },
        /** 성공 상태 색상 (초록색 계열) */
        success: {
          500: '#10b981',  // 기본 초록색
          600: '#059669',  // 진한 초록색
        },
        /** 경고 상태 색상 (주황색 계열) */
        warning: {
          500: '#f59e0b',  // 기본 주황색
          600: '#d97706',  // 진한 주황색
        },
        /** 위험 상태 색상 (빨간색 계열) */
        danger: {
          500: '#ef4444',  // 기본 빨간색
          600: '#dc2626',  // 진한 빨간색
        }
      }
    },
  },
  plugins: [],
}
